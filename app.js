/* ============================================================
   DEPRECATED — this file is no longer used.

   The app was split into multiple smaller files to support the
   expanded feature set (colors/language/currency/purchasing/users):

     data.js -> i18n.js -> app-state.js -> app-pages-core.js ->
     app-pages-more.js -> app-purchasing.js -> app-settings.js ->
     app-shell.js

   index.html now loads those files instead of this one. This file
   is kept only so nothing 404s if something still references it;
   it intentionally does nothing.
   ============================================================ */
